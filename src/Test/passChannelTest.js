/* eslint-disable linebreak-style */

const ChannelTest = async (channel) => {
  //  console.log(channel);
 // if (channel && channel.isText()) {
    await channel.send('test');
  //}
};

export default ChannelTest;
